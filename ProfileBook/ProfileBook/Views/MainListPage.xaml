<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             xmlns:resource="clr-namespace:ProfileBook.Resources;assembly=ProfileBook"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="ProfileBook.Views.MainListPage"
             Title="{Binding Title}"
             x:Name="this">

    <ContentPage.ToolbarItems>
        <ToolbarItem Command="{Binding LogOutCommand}"
                     Text="{x:Static resource:Resource.LogOutText}"
                     IconImageSource="ic_exit_to_app.png"
                     Order="Primary"/>
        <ToolbarItem Command="{Binding SettingsTapCommand}"
                     Text="{x:Static resource:Resource.Settings}"
                     IconImageSource="ic_settings.png"
                     Order="Primary"/>
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackLayout Grid.Row="0">
                <ListView ItemsSource="{Binding Profiles}"
                          IsVisible="{Binding IsListVisible}"
                          SelectedItem="{Binding SelectedProfile}"
                          HasUnevenRows="True">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <ViewCell.ContextActions>
                                    <MenuItem Command="{Binding BindingContext.EditCommand,
                                        Source={x:Reference this}}"
                                              CommandParameter="{Binding .}"
                                              Text="{x:Static resource:Resource.Edit}"/> 

                                    <MenuItem Command="{Binding BindingContext.DeleteCommand,
                                        Source={x:Reference this}}"
                                              CommandParameter="{Binding .}"
                                              Text="{x:Static resource:Resource.Delete}"/>
                                </ViewCell.ContextActions>
                                <ViewCell.View>
                                    <Grid Padding="15">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="2*"/>
                                        </Grid.ColumnDefinitions>

                                        <ffimageloading:CachedImage Grid.Column="0"
                                                                    Source="{Binding ProfileImagePath}"
                                                                    HeightRequest="70"
                                                                    DownsampleToViewSize="True"
                                                                    CacheDuration="14"
                                                                    LoadingPlaceholder="pic_profile.png"/>

                                        <StackLayout Grid.Column="1"
                                                     Margin="10, 0">
                                            <Label Text="{Binding NickName}"
                                                   FontAttributes="Bold"/>
                                            <Label Text="{Binding Name}"/>
                                            <Label Text="{Binding InsertionTime}"
                                                   FontAttributes="Italic"/>
                                        </StackLayout>
                                    </Grid>
                                </ViewCell.View>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Label IsVisible="{Binding IsLabelVisible}"
                       Text="{x:Static resource:Resource.NoProfilesText}"
                       VerticalOptions="CenterAndExpand"
                       HorizontalOptions="CenterAndExpand"/>
            </StackLayout>

            <ImageButton Grid.Row="0"
                         Command="{Binding AddCommand}"
                         Source="ic_add.png"
                         HorizontalOptions="End"
                         VerticalOptions="End"
                         WidthRequest="70"
                         HeightRequest="70"
                         Margin="20"
                         Padding="20"
                         BorderColor="Black"
                         BorderWidth="3"
                         CornerRadius="35"
                         BackgroundColor="#1E90FF"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>